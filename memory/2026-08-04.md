# Memory — 2026-08-04

## Session Events (evening, Gabriel DM)

### DeepSeek token spend (asked by Gabriel 21:20)
- **7-day average: ~$1.07/day. 14-day average: ~$0.84/day.**
- Daily range over last 14 days: $0.41–$2.01. Trending up (Aug 2: $1.36, Aug 3: $2.01).
- Typical day: ~110–140 model calls, ~1.5–2.6M input tokens + ~100–340K output, heavy cache reads.
- Today (Aug 4) so far: ~$0.45. Computed from agent trajectory files (`model.completed` records), priced with flash rates (in $0.14/M, out $0.28/M, cacheRead $0.028/M).
- **CRITICAL: DeepSeek account balance ≈ $1.89 — nearly depleted.** At ~$1/day burn ≈ 2 days of runway.
- Email Monitor cron failing repeatedly: "Provider deepseek is in cooldown (suspending lanes) (billing)" / 402 Insufficient Balance. Anthropic fallback also dead (no credit). Needs top-up ASAP or daily pipeline will die.

### Cron jobs model audit (asked by Gabriel 22:12)
- All 5 jobs run on **DeepSeek Flash (v4-flash)** — zero Pro usage. No job declares a model override; all inherit agent default (deepseek-v4-flash). Pipeline scripts don't hardcode a model.
- Jobs: Daily Pipeline 01:00, Pipeline Retry 04:00, Deploy Health Check 08:45, Daily Stats 21:50, Email Monitor (hourly, fallback flash → Claude Sonnet 4).
- **01:00 pipeline is NOT LLM-written** — `scripts/ink_review_writer.py` is 883 lines of pure Python, **zero LLM API calls** (grep for requests/urllib/openai/anthropic/httpx/deepseek = 0 matches). Reviews are script-generated from scraped review text (sentiment extraction + templating + quality gates). LLM only orchestrates the cron turn (~15–30K tokens/run). Hand-written reviews (e.g. Breville Grind Control 08:00) are the only LLM-authored ones.

### Site issues Gabriel flagged (22:22) + fixes (22:46 "Fix")
1. **Main page vs Today page don't tally; main page all coffee machines.**
   - Root cause A: **Homepage HTML is stale** — `/` served with `cache-control: public, s-maxage=604800` (7-day CDN cache), age ~41h at check. Homepage showed July 24–26 content while today's 08-04 reviews are live individually (200 OK) and /today/ (max-age=0) is fresh.
   - Root cause B: **Today page template bug** — `layouts/today/list.html` only included 6 categories (kitchen, coffee, home-office, home-improvement, luxury-beauty, pet-supplies); missing furniture, patio-lawn-garden, sports-fitness, toys-games.
   - Root cause C (by design, reads wrong): homepage "Editor's Favourite" column sorts last-30-days by price desc → espresso machines always win.
2. **Latest additions = today page items (08-04 script-generated batch) — quality below main page's curated/expensive items.** Correct read by Gabriel; root cause = script writer + queue serving cheap filler ASINs.
3. **Repeated "The Verdict" heading on review pages** — `layouts/_default/single.html` line 59 hardcodes `<h2>The Verdict</h2>` AND `ink_review_writer.py` writes `## The Verdict` into markdown body → double heading. 210 content files affected.

### Fix status (AS OF FLUSH — NOT COMMITTED/PUSHED YET)
- ✅ `static/_headers` created: `/*` → `Cache-Control: public, max-age=0, must-revalidate`; `/images|css|js/*` → max-age=86400. (verified)
- ✅ `layouts/today/list.html`: 4 missing categories added to slice (verified via read).
- ✅ `scripts/ink_review_writer.py`: `## The Verdict` emission removed (claimed — re-verify on next session).
- ✅ 210 content files stripped of `## The Verdict` headings via regex `^#{1,3} The Verdict\s*$` (claimed — re-verify count = 0 on next session).
- ⏳ **deploy.yml cache-purge step NOT added** — last action was reading `.github/workflows/deploy.yml` (Deploy step uses wrangler@4.113.0, token secret `CLOUDFLARE_API_TOKEN`, account `ecd6d8f6b5e56cccb482101a3ad6212d`). Plan: add Cloudflare purge step (or wrangler pages deployment purge-cache) after deploy, then commit + push + verify live (homepage fresh, /today/ has all 10 cats, single "The Verdict" heading).
- Git state at flush: HEAD = ae7e692c1 (08:00 batch, 16 reviews today). Uncommitted: CONVERSATION_LOG.md, briefings/.pipeline_status, data/asin_queue.json, data/processed_asins.json, memory/.dreams/events.jsonl, new briefing files, plus the fix files above.

## Queue / pipeline state (unchanged from earlier today)
- Today's batch done: 15 reviews (01:00) + 1 Breville Grind Control (08:00), QA passed, pushed.
- 13 new briefings pending (12 coffee + 1 furniture) for tomorrow's 01:00 run.
- Deploy health check 08:45 OK. Daily stats posted 21:50 (Mon 03 Aug: 10 pageviews).

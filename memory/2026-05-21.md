# 2026-05-21

[00:00 SGT] Checkpoint: midnight checkpoint
   → Pending items synced to HANDOFF.md
[08:00 SGT] Checkpoint: morning checkpoint
   → Pending items synced to HANDOFF.md
[16:00 SGT] Checkpoint: afternoon checkpoint
   → Pending items synced to HANDOFF.md
## Session: 21:00 SGT — Gabriel reported reviews not going out; full pipeline rebuild

### Problem
- 15 briefings (10 unique, 6 duplicates) sitting unprocessed since 08:00
- 09:00 "Ink Review Retry" cron using relative paths → isolated session couldn't find files → reported "zero unprocessed briefings" → silent failure
- Delivery: none meant nobody ever got notified

### Fix applied
1. Rewrote 09:00 cron with absolute paths, git-log-based "already published?" check, always posts to ops group
2. Added 16:00 afternoon catch-up cron with same pipeline
3. Both crons now include ASIN scraping + briefing generation — replacing Signal's role
4. Sub-agent spawned to write the 10 unique reviews from today's existing briefings

### Going forward
- Ink runs the full pipeline: ASIN queue → scrape Amazon → generate briefings → write 10 reviews → commit+push → report to ops group
- Two daily runs: 09:00 and 16:00 SGT
- Always posts status to GLYC Digital Ops group (telegram:-1003250348876)
- Target: 10 reviews/day, ~3-4 per category (coffee, kitchen, home-office)

### Pipeline ownership transfer
- Gabriel confirmed: Ink now owns the full review pipeline end-to-end
- Signal is no longer relied upon for briefing generation
- 09:00 & 16:00 crons handle: ASIN queue → scrape Amazon → briefings → 10 reviews → commit → push → ops report
- Signal's `run_daily.py` logic + `amazon_scout.py` (free, no Apify) replace Apify-based scraping

### Commands/logic absorbed from Signal
- `run_daily.py` (ASIN picking logic) — ported to 09:00/16:00 cron instructions
- `refill_asin_queue.sh` — runs automatically when queue is low
- `amazon_scout.py` — direct Amazon scraping (no Apify credits needed)
- `asin_verify.py` — ASIN validation before scraping

*.log
.DS_Store
.hugo_build.lock
public/
resources/
scripts/.apify_config.json
drafts/
/reviews/
memory/.dreams/
memory/dreaming/
clients/

# ShelfWatch price/rating history store (regenerated by shelfwatch.py --seed)
data/history/
scripts/.env

# Temp shelfwatch seed data (breaks Hugo data loader)
data/history_tmp/

# LLM rewriter checkpoint (moved out of data/ — Hugo scans data/)
state/
